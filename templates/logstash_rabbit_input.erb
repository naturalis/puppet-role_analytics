  rabbitmq {
    auto_delete => false
    durable => false
    exchange => "logstash-exchange"
    exclusive => false
    host => "<%= @ipaddress %>"
    user => 'logstash'
    password => '<%= @rabbit_logstash_password %>'
    queue => "logstash-queue"
    tags => ['from_rabbitmq']
    type => "logging"
  }

