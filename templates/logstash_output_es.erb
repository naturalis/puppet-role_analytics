}
output {
  if "suricata" in [tags] {
    elasticsearch {
      cluster => <%= "@cluster_name" %>
      index   => "logstash-suricata-%{+YYYY.MM.dd}"
    }
  } else {
    elasticsearch {
      cluster => <%= "@cluster_name" %>
    }
  }
}
